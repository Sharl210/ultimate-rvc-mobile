# Ultimate RVC Mobile Project Notes

## Build Verification

- 本项目以后做 APK 编译验证时，默认编译发布版 APK，不再以 debug APK 作为最终验证口径。
- 推荐发布版验证命令：`flutter build apk --release`。
- 本机已验证可用 Android SDK 路径为 `/home/harl/android-sdk`，项目内 `android/local.properties` 应保持 `sdk.dir=/home/harl/android-sdk`。
- 本机 Gradle 构建应使用 Java 17，项目内 `android/gradle.properties` 应保持 `org.gradle.java.home=/usr/lib/jvm/java-17-openjdk-amd64`。
- [2026-05-04 CST] 用户明确要求恢复 Kotlin LSP 的使用，并删除此前“不要再尝试 Kotlin LSP”的临时妥协。当前已验证 `lsp_diagnostics` 可扫描 `android/app/src/main/kotlin/com/ultimatervc/mobile` 下 11 个 Kotlin 文件；这次返回 0 个错误和若干既有警告（主要是 Android API deprecation、参数未使用和变量遮蔽）。后续 Kotlin 改动应优先跑 Kotlin LSP 降低试错成本，但最终仍必须用 `flutter build apk --release` 做发布版构建验证；Dart 仍可使用 `flutter analyze` 和相关 Flutter 测试验证。

## Device Install Workflow

- [2026-05-01 20:12 CST] 用户已在 WSL2 环境接好 Android 真机 ADB，设备可通过 `/home/harl/android-sdk/platform-tools/adb` 访问，当前已知设备为 RMX5200，且 `adb shell` 具备 root shell 权限。此前曾用该连接做过安装、启动、截图、点击和 logcat 抓取验证，并在真机上发现过 `file_picker` 对 `.pth/.index` 扩展名过滤失败的问题。
- 后续本项目里用户提到 ADB、真机、推送到手机或安装 APK 时，默认语义不是“自动化测试”，而是将当前发布版 APK 安装到手机即可。默认安装命令为：`/home/harl/android-sdk/platform-tools/adb install -r -d build/app/outputs/flutter-apk/app-release.apk`。
- [2026-05-06 CST] 用户更新了交付偏好：以后做 release APK 安装时，不要先检查 `adb devices`，也不要在无设备时自动走网页上传兜底；如果当前没有设备，就等用户手动连上后再执行安装。默认只保留 ADB 安装动作本身，不主动做设备探测、网页上传、启动应用、截图点击、抓 logcat 或清数据。只有用户明确要求时，才进行额外安装/交付动作。
- [2026-05-06 CST] 用户在继续梳理“继续未完成 / 文件清理 / 保存语义”时，进一步明确了本项目所有工作文件都要按**单例模式**管理，不能越积越多。这里的“切换”包括但不限于切换输入音频、切换录制音频、切换音色模型、切换索引文件，以及开始一轮新的生成/处理。用户真实意图是：只要发生切换或开始新的处理，就必须**先清理以往未被用户手动保存的历史文件**，再执行切换或新的处理；如果清理失败，应直接报错并停止后续切换/处理，不能带着脏历史继续跑。清理范围不只包含音频推理模式的旧输入文件、旧输出文件、分块缓存和 manifest，也包含变声器模式的录制文件、处理中间文件、输出文件，以及模型/索引相关联的旧单例工作文件。与此同时，用户手动点击的“保存”语义应被视为“另存为”：这类文件属于用户明确要求保留的额外产物，不应继续放在应用自己的工作数据目录里，也不参与下一轮“未保存历史文件”清理；工作目录里的文件一律按单例临时文件处理，新的覆盖旧的，切换前先删旧的。后续只要实现任何与音频选择、模型选择、索引选择、重新生成、继续未完成、变声器录制/处理/保存相关的逻辑，都必须先对照这条规则，确保不存在多份未保存历史文件残留或切换后还沿用旧文件的情况。

## Cross-Mode Fix Scope

- [2026-05-04 CST] 本项目在排查和修复推理、音频、内存、性能或导出问题时，用户要求默认采用“跨模式联动检查”策略：只要修了某一个模式，就必须顺手检查另外两个模式是否也存在同类问题，或是否可以一并复用同一个修复。这里的“三个模式”指音频推理、实时推理、变声器模式。触发背景是本次连续调试中，实时推理、离线长音频、变声器都暴露出相似的高内存占用、停止后内存不回落、以及链路一致性问题，用户不希望再出现只修单一模式、遗漏另外两个模式的情况。后续执行时，除非用户明确要求只改单点，否则默认把修复范围至少扩展为“主改模式 + 另外两个模式的存在性核查 + 能共用则一并修复”的闭环，并在汇报中明确说明另外两个模式是否已检查、是否同样受影响、是否已同步处理。

## Inference Performance Context

- [2026-05-04 21:21:28 CST] 用户确认当前性能排查的关键背景：此前所谓 root 性能模式已经被“阉割”，分块数同时处理也已经被“阉割”，后续不能再把 root 性能策略或分块并发当成可用优化抓手。本次用户反馈的性能差异（原约 32 秒，独立进程后约 39 秒）是基于“基础性能方案”的同口径比较；当时阉割后已经等价于直接隐式使用基础性能模式。因此后续定位 32s→39s 的回退时，默认优先怀疑独立进程 IPC 和生命周期成本，例如 bind/unbind、Messenger、Bundle、进程冷启动/退出、进度消息频率、线程切换和数据传递方式，而不是继续补 root 性能模式、恢复 root 优化或恢复分块并发。实时推理仍需接入独立进程策略，但实现时要避免每个 hop 绑定服务、重复初始化模型或高频低效 IPC。

## Debugging Memory

- [2026-05-06 CST] 变声器悬浮窗播放路径记忆：用户明确说明“正式播放其实已经没用了，那个路径很早之前就被砍掉了”。后续处理变声器悬浮窗的红色状态、保存手势、播放/暂停文案和事件分支时，不要只看旧的 `PLAYING`/正式播放分支；当前实际需要优先覆盖的是试听播放路径 `TRIAL_PLAYING` 以及对应暂停态 `TRIAL_PAUSED`。如果要保留旧 `PLAYING` 分支可以作为兼容兜底，但不能把它当作主要路径，也不能因为只实现了 `PLAYING` 就声称“红色状态已支持”。

- [2026-05-06 CST] 用户在 `/home/harl/ultimate-rvc-mobile/ultimate_rvc_mobile` 这轮“继续未完成 / 分段落盘 / file_picker 目录 / TEMP 语义”修复中，进一步明确了一个执行约束：以后**每次列 todo 之前**，都要先重读一遍用户刚刚说的话，确认最新补充的口径、反例、边界条件和真正诉求都已经被考虑进去，再写 todo 或更新 todo。这里的“重读”主要针对用户的自然语言短消息；大段代码、大段报错、大块日志可以按需抓重点，不要求逐字重复阅读。触发背景是这次施工里我多次在没有真正吸收用户最新表述前就更新 todo 和推进实现，导致按钮位置、目录语义、分段行为、显示条件连续做偏。后续在本项目里，只要要新建或更新 todo，就默认先检查本轮用户最近的口径补充是否已经落实到任务项里；如果没有，先修正任务理解，再动手。

- [2026-05-06 CST] 用户又补充了一条回复习惯约束：以后**每次正常回复前**，都要先重读一遍用户刚发的短消息，再组织回复和后续动作；例外只有大段代码、大段报错、大块日志这类长文本，不要求逐字重读。触发背景是这轮施工里我多次没有真正消化用户刚刚补充的要求，就直接继续解释、下结论或推进，导致对按钮位置、分段语义、目录职责的理解一再偏掉。后续在本项目里，只要是用户新发的自然语言短消息，默认先重读一遍再答，防止漏掉关键词、否定词、位置要求和前后文修正。

- [2026-05-06 CST] 用户进一步明确“记到文件里”的具体载体：不是项目里共用一个泛化的 `用户输入.txt`，而是**一个主 session 拥有自己独立的输入记录 txt**。触发背景是这轮施工中我先建了项目级通用 `用户输入.txt`，用户随后纠正“一个主session拥有自己的.txt”。用户真实意图是：会话输入记录要按主 session 隔离，避免不同轮次、不同时段、不同问题的用户输入混在一个公共文件里。后续如果用户再次要求“把我前面说的话记在文件里”，默认应创建或追加当前主 session 专属的输入记录文件，例如 `主session-YYYY-MM-DD-用户输入.txt` 这类命名，而不是继续写到一个项目共用的总文件。

- [2026-05-06 CST] 用户要求把这轮修复“计划”写入项目记忆。这里的“计划”不是泛泛一句“修继续未完成”，而是当前已明确的完整施工口径：第一，文件选择器选择出来的源文件真实来源目录是 app 私有 `cache/file_picker/<时间戳>/...`，后续任何导入、显示、清理、引用计数都必须以这个事实为起点，不能再把 `files/FILE_PICKER` 当成真实来源；第二，目录语义必须分三层，`cache/file_picker` 只放用户选中的源文件，`files/TEMP/...` 只放处理中间文件（manifest、chunks、converted chunks、处理中间产物），导出文件必须走导出目录，不能和 TEMP 混用；第三，超过 **30 秒** 或 **5MB** 的音频，在音频推理和变声器相关处理链路里都必须进入一致的分块处理逻辑，而且分块结果必须作为真实文件写进文件系统，不能只在内存里算完；第四，`继续未完成` 按钮属于音频推理当前模式页面，位置必须在“开始生成”按钮下面，并且只有当 `TEMP/audio_inference` 下面真的存在对应任务的 manifest 和分段中间文件时才显示，不能做成常驻按钮，也不能只凭内存态或空元数据显示；第五，“开始生成”的语义就是全新生成，不能和“请重新生成”这种错误提示打架，继续未完成和全新生成必须是两条清晰分开的路径。后续凡是继续这条修复线，默认先对照这条计划记忆，防止再次出现只改存储、不改读取，只改按钮、不改分段落盘，或把中间文件和导出文件混为一谈的错误。

- [2026-05-06 CST] 用户在 `/home/harl/ultimate-rvc-mobile/ultimate_rvc_mobile` 连续纠正“继续未完成 / file_picker 缓存目录 / TEMP 中间文件 / 分段落盘”这轮修复时，进一步明确了一个工作方式约束：以后在这个项目里，用户在当前对话中连续提出的关键要求、口径修正、目录语义、按钮位置、分段阈值、显示规则等内容，不能只靠临时上下文记住，也不能只摘一两句抽象总结；必须优先按项目级记忆持续补充到本文件，至少覆盖触发背景、用户原意、当前已确认事实、之前做错的地方、后续实现边界。触发原因是这次我多次误读用户要求，例如把文件选择目录误做成 `files/FILE_PICKER` 而真实来源是 app `cache/file_picker`，把变声器错误加进“继续输入音频”界面，把“继续未完成”按钮位置和显示条件做错，以及在未确认事实前对分段落盘和目录清理下错结论。用户真实意图是：只要他在这类多轮施工里连续说了很多句关键要求，就要把这些要求及时沉淀到项目记忆里，避免代理后续继续漏读、误读或反复推翻。后续在本项目里遇到用户明确补充口径、尤其是带“我已经说得很清楚了”“每一句话都要记在文件里”这类强调时，默认应把这些内容写入项目级 `AGENTS.md`，而不是只在回复里口头确认。

- [2026-05-05 CST] HuBERT ONNX Runtime `Where` 崩溃排查的教训：AppErrorsTracking 和本机 ONNX Runtime 复现都指向 `/layers.0/self_attn/Where` 报错 `Can broadcast 0 by 0 or 1. 8 is invalid`。当时一度把修复方向误判成 `padding_mask` 值语义问题，改成“原始 `audio16k.size` 输入 + 全 false mask”，但本机用 `assets/models/hubert.onnx` 复现证明 `length=2880`、全 false mask 仍会触发同类崩溃，说明根因不是 mask 的 true/false 值，而是输入长度经过 HuBERT 卷积下采样后的帧数与模型内部 padding/mask 切片边界不匹配。进一步实测和 ONNX 图追踪确认：模型会按卷积后的 HuBERT frame count 裁剪 `padding_mask`，若输入长度刚好能被 frame count 整除会产生空 mask；若 frame count 为奇数，另一条特征分支会被位置卷积 padding 成偶数帧，也会和 mask 分支错位。后续凡是再处理 HuBERT/ONNX `Where`、attention mask、广播长度、`8 is invalid`、`4 is invalid`、短音频或实时 hop 相关崩溃时，禁止回到“原长输入 + 全 false mask”或未经验证的固定 160/640 对齐规则；默认应保留并验证 `alignHubertInputLength(audio16k.size)`：补齐到至少 `HUBERT_MIN_SAFE_INPUT_LENGTH = 801`，按 HuBERT 卷积核/步幅计算 frame count，寻找 frame count 为偶数且 `paddedLength % frameCount != 0` 的最小安全长度，然后补齐音频尾部、只把 padding 尾部标记到 `padding_mask`，并用静态测试和真实 `hubert.onnx` 复现/回归验证共同收口。
- [2026-05-05 CST] 本项目的关键取舍和阉割功能必须写入项目级记忆，不能只靠对话上下文记住。尤其是 root 性能模式已经阉割、分块数同时处理已经阉割、release APK 是最终验证口径、Kotlin LSP 需要恢复使用、默认安装 release APK 到真机、实时 `delayBufferSeconds = 10.0` 不要随手改、`noiseGateDb` 默认应为正数 `35.0` 这类决策，后续修改相关代码前必须先查本文件。除非用户明确要求重新评估或恢复被阉割功能，否则不要把这些旧功能当作优化抓手，也不要在修性能或崩溃时误启用它们。
- [2026-05-05 CST] 用户在安装 HuBERT 修复版后反馈：实时推理已经有声音，但“不管怎么说话就只是呜两秒就没反应”，离线音频推理只优化约 2 秒仍达不到旧基线。本轮调查结论是：实时会话没有证据显示每个 hop 重连服务或定时自动停止；高概率是实时短窗先在输入侧经过 `applyInputAudioFilters`，然后尾段输出又被 `applyOutputAudioFilters` 二次门限/降噪，叠加短窗 `mixRms` 波动、输出队列丢块或 `AudioTrack.write()==0` 静默 return，表现成“会话还在跑但输出近静音/只剩低频嗡声”。当前最小修复决策是：离线整段生成继续保留输出滤波和降噪，但实时 `RvcStreamingSession.inferPcm16` 不再对 `currentAudio` 做第二次 `applyOutputAudioFilters`，只保留输入侧过滤，避免把短 hop 的有效语音尾段再次压没。后续如果实时仍异常，优先看 `mixRms` 短窗增益、`outputChannel.trySend` 丢块计数、`AudioTrack.write()` 返回 0 的处理，不要先怀疑 HuBERT padding 或恢复 root/分块并发。
- [2026-05-05 CST] 离线音频推理仍慢于旧基线的关键判断：独立 `:inference` 进程的 bind/unbind、Messenger/Bundle 不是主要数据搬运瓶颈，因为请求只传路径和标量、返回只传输出路径；更像主因的是 one-shot 离线路径虽然让服务保温 30 秒，但 `InferenceProcessService.handleInferAudio` 每次仍临时 `RvcInferenceEngine(...).infer(...)`，而 `RvcInferenceEngine.infer` 每次都会重新创建 HuBERT、RMVPE 和音色模型 ONNX sessions，服务保温并没有复用模型 session。尾部服务端与插件端的 `System.runFinalization()`/`System.gc()` 也可能贡献固定尾延迟。后续要继续追离线 32s 基线时，优先设计“服务保温期间复用离线 ONNX sessions”的方案并同时评估 native heap 释放边界；不要再把优化重点放在 root 性能模式、分块并发、Binder 传大对象或 HuBERT padding 上。
- [2026-05-05 CST] 用户随后反馈实时仍然“只会啊一句就没动静”、独立进程版本比上个版本反慢约 5 秒、重新生成后结果页偶发点不开。本轮最小修复结论如下：第一，实时 `RvcStreamingSession.inferPcm16` 中 `mixRms(formantAudio, mono16k, rmsMixRate, sampleRate)` 是错误时间基准，因为 `mono16k` 已是 16k HuBERT 输入，传输出采样率会让短窗 RMS 包络错位，已改为 `HUBERT_SAMPLE_RATE`；后续处理实时短窗 RMS、响度匹配或“只出一声/一段后静音”时，默认先检查输入与输出采样率是否混用。第二，`InferenceProcessService.writeAudioTrackFully` 曾对 `AudioTrack.write(...) == 0` 直接 `return`，这会把播放设备暂时不接收数据伪装成正常写完，导致实时输出链路静默断流；后续不得再吞掉 0 写入，应让 `check(written > 0)` 暴露异常并通过实时状态峰值字段判断是推理静音还是播放写入失败。第三，远程离线成功路径在服务保温 30 秒前强制 `requestNativeMemoryCleanup()` 会贡献固定尾延迟，已移除 `handleInferAudio` finally 中的强制 GC，只保留 warm shutdown；实时 stop 和主插件/变声器完成后的内存清理仍按各自生命周期保留。第四，结果页偶发点不开不是 `ResultScreen` 按钮本身的问题，而是旧的 `GenerateScreen._generate()` 异步 infer 完成后还能调用 `widget.onGenerationComplete(...)`，污染 `_generatedOutputPath`、`_audioStepIndex` 和持久化状态；已通过 `_activeGenerationRequestId` 与 `mounted` 检查丢弃过期进度、完成和失败回调。后续凡是改生成页、重新生成、结果页导航或长耗时异步回调，都必须保留“本次生成令牌 + mounted 检查”，禁止裸调用 `onGenerationComplete`。
- [2026-05-05 CST] 用户又反馈“音频推理直接完全无限大的时间了，独立进程没拉起来”，同时实时推理仍“只会露一声”。本轮真机证据显示当时 `adb shell ps -A` 只有主进程 `com.ultimatervc.mobile`，没有 `com.ultimatervc.mobile:inference`，`dumpsys activity services` 也没有活跃 service；这说明仅凭 release 构建通过不能证明远程推理运行时闭环。根因之一在 Dart 桥：`lib/services/rvc_bridge.dart` 的 `infer()` 在原生 `invokeMethod('infer')` 已返回后，还等待可能因为 EventChannel 竞态丢失的 100% 进度事件，导致前端“生成用时”无限增长；后续音频推理完成应以 MethodChannel 返回值为准，进度流只用于展示，不得作为完成门闩。根因之二在 `RemoteInferenceClient.infer`：服务连接后没有总请求超时，`onServiceDisconnected`/`onBindingDied`/`onNullBinding` 也没有把断连转成错误并释放 `resultLatch`，远端进程中途死掉时客户端会无限等待；后续远程进程 IPC 必须有请求级超时和 binder 断连失败传播。实时方面，本轮进一步确认短 hop 不能复用离线完整输入滤波，尤其不能在实时输入上启用 `applyAdaptiveDenoiseGate`，否则第一声能过、后续轻声和尾音会被短窗自适应噪声地板压成近静音；当前策略是实时使用 `applyRealtimeInputAudioFilters`，只保留音域过滤和基础 noise gate，离线整段仍保留完整输入/输出滤波。`AudioTrack.write()==0` 也不能直接失败或静默 return，应做有限短重试后再暴露异常。后续排查实时“一声后没了”时，优先看 `last_input_peak`、`last_raw_output_peak`、`last_output_peak`、`output_channel_drop_count` 和 `audio_track_write_count`，不要只看 UI 文案，也不要再把 100% 进度事件当作生成完成条件。
- [2026-05-05 CST] 用户最终明确要求不要继续追求真流式实时，而是把实时推理回退到之前“整体能跑，但大约每 6 秒会卡一下”的旧体验，同时在实时页右上角加入更明确的警告说明。这个决定的真实含义不是做成“一次录 6 秒的假演示模式”，而是保留实时入口和现有参数页结构，但把实时默认参数和窗口策略回退成大窗口整块处理：`realtimeSampleLength = 6.0`，`crossfadeLength = 0.0`，`extraInferenceLength = 0.0`，`delayBufferSeconds = 0.0`，并把 `InferenceProcessService` 的 `realtimeHopFrames` 直接退回 `inferenceWindow.size`，从而恢复旧的“约 6 秒一卡”的体感。后续如果再有人提实时推理优化，默认不能再把这个页面当作稳定可用功能，而应先记住它现在是“保留入口但明确降级”的状态。页面右上角的告警必须保留，标题是“手机端限制”，文案必须明确说明“仅供演示”“无法指定输入输出设备”“没有实际用途”，不要再用含糊帮助文案弱化限制。与此同时，离线独立进程这次没有继续回滚 Dart 完成条件、RemoteInferenceClient 超时/断连传播、GenerateScreen 请求令牌这类安全修复，因为它们解决的是无限等待和状态污染，不属于用户要求撤回的“性能策略”；以后如果再做离线性能回退，不能误删这些安全修复。
- [2026-05-05 CST] 用户在合并 `audio-enhancement-denoise` worktree 回主分支时纠正了一次关键误读：上条“实时推理降级/回退”记忆描述的是当时另一条开发线的回退版本，不代表当前主分支后续要跳过实时输出降噪。当前合并目标明确要求：实时推理也要走完整输出降噪链路，即 `RvcStreamingSession.inferPcm16` 的 `currentAudio` 必须继续进入 `applyOutputAudioFilters(currentAudio, noiseGateDb, outputDenoiseEnabled, vocalRangeFilterEnabled, sampleRate)`，再 `floatToPcm16(resizeAudio(filteredAudio, outputFrames))`。后续处理主分支实时音频、降噪、电音伪影、包络噪声或合并该 worktree 时，禁止再把“降级线”当成当前主线要求，也不能为了规避短窗风险擅自移除实时输出完整降噪；如果实时输出降噪导致问题，必须先用状态指标和真机试听定位，再按用户确认调整。
